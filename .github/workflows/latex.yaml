name: "Build LaTeX Report"

on: [push]

jobs:
  build-latex:
    runs-on: ubuntu-latest
    steps:
    - "Build Latex Report"
      uses: xu-cheng/latex-action@v3
      with:
        root_file: "report/Project Report.tex"